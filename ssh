git configuration:)

Set Up Git:
git config --global user.name "Your Name"
git config --global user.email "youremail@domain.com"

other commands
git clone (url)
git add (file names)
git commit -m "(committed message)
git push -u origin master
git remote add origin master (url)
$ git push origin master
$ git pull origin master
$ git push origin master -f
$ git log
$ git reset --hard <commit number>



eval $(ssh-agent -s)

Add your SSH private key to the ssh-agent. If you created your key with a different name, or if you are adding an existing key that has a different name, replace id_rsa in the command with the name of your private key file.

ssh-add ~/.ssh/id_rsa


1.$ ssh-keygen -t rsa -C "email"
<put password jai@12345>
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa


3.$ls .ssh
4.$cat .ssh/id_rsa.pub //use to show keys
5.clip < ~/.ssh/id_rsa.pub
5.copy and paste key in online github
6.and create a repo in git and locally as well change from ssh to https
7.mkdir name
8.cd name
9.git init
10.git config --global user.name "Your Name"
11.git config --global user.email "youremail@domain.com"
12.//$ git add readme.md //optional
13.git status
14.git add .
15.git commit -am ‘<add note>’
16.$ git status //optional
17.$ git remote add origin git@github.com:mjhea0/git-commands.git
18.git push origin master
